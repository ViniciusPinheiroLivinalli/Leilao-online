import json
import struct

def enviar(sock, tipo, dados):
    # Monta e envia mensagem com cabeçalho de tamanho
    pass

def receber(sock):
    # Lê cabeçalho + payload e retorna dicionário
    pass